# World — expired FIGURE, same pack, no new slug

Spoken: licence expired. the agent stays silent.

1. Read `langue` on the card. That is the BCP 47 pack (`fr-CA` · `en` · `es-MX` · `en-NG`).
2. `python3 figure.py juger examples/expiree-silence.figure.json`
3. `decision: deny` + `silence: true`. Person is not fake. Speak of, not as.
4. Same host. Never a second grok.me.

Preview is not a receipt. Unforge does not sign.
Hôte : https://acorn-royal-dune-blend.grok.me
